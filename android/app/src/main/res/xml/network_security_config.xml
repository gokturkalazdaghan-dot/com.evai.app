<?xml version="1.0" encoding="utf-8"?>
<!-- android/app/src/main/res/xml/network_security_config.xml -->
<network-security-config>
    <!-- Production: yalnızca HTTPS. -->
    <base-config cleartextTrafficPermitted="false" />

    <!-- Yerel geliştirme (10.0.2.2 = emülatörden host makineye erişim)
         yalnızca debug build'lerde cleartext'e izin verir. Bu domain
         production APK/AAB'de erişilemez olduğu için güvenlik riski
         taşımaz. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
